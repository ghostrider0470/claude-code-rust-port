# Claude Code Rust Port - Implementation Plan

## Active Goal

Build a Rust-native Claude Code-style CLI/runtime that Hamza can use as a primary tool, while keeping the underlying components clean enough to donate useful pieces into Horizon later.

## Phase 1 - Architecture Capture

- [x] Review mirrored workspace and snapshot metadata
- [x] Identify major architectural surfaces
- [x] Write `ARCHITECTURE.md`
- [x] Write this implementation plan

## Phase 2 - Rust Workspace Bootstrap

- [x] Create workspace `Cargo.toml`
- [x] Create crates:
  - [x] `harness-core`
  - [x] `harness-session`
  - [x] `harness-tools`
  - [x] `harness-commands`
  - [x] `harness-runtime`
  - [x] `harness-cli`
- [x] Add shared dependencies (`serde`, `serde_json`, `clap`, `thiserror`, etc.)
- [x] Ensure `cargo check` passes

## Phase 3 - Core Types

- [x] Implement ids and metadata types
- [x] Implement usage/accounting types
- [x] Implement error model
- [x] Implement event enums

## Phase 4 - Session + Transcript

- [x] `SessionState`
- [x] `TranscriptEntry`
- [x] append/replay/compact
- [x] persistence to disk
- [x] session reload

## Phase 5 - Registries

- [x] Tool registry with metadata
- [x] Command registry with metadata
- [x] Search/filter/list APIs
- [x] Permission policy layer

## Phase 6 - Router + Runtime

- [x] Prompt tokenization
- [x] Match scoring
- [x] Deterministic ranking
- [x] Turn processor
- [x] Event emission
- [x] Session persistence after turn

## Phase 7 - CLI

- [x] `summary`
- [x] `route <prompt>`
- [x] `bootstrap <prompt>`
- [x] `resume <selector> <prompt>` — append a new turn to an existing persisted session; `<selector>` accepts raw `session_id`, `latest`, or `label:<name>` routed through the shared selector-resolution path; the resumed turn is appended to the same persisted session file, `updated_at_ms` is refreshed, and the sibling transcript is rewritten so `turn_index` ordering is extended in place; output is machine-readable JSON that identifies the resolved persisted `session_id` via `resumed_session_id` rather than the typed selector string and exposes the appended turn index; selector failure semantics stay deterministic (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `tools`
- [x] `commands`
- [x] `session-show <selector>` — inspect the persisted state of a single session; `<selector>` accepts raw `session_id`, `latest`, or `label:<name>` routed through the shared selector-resolution path; output is machine-readable JSON that surfaces the actual resolved `session_id` rather than the typed selector string; selector failure semantics stay deterministic (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-show <selector>` — inspect the persisted transcript for a single session; `<selector>` accepts raw `session_id`, `latest`, or `label:<name>` routed through the shared selector-resolution path; output is machine-readable JSON that restates the owning `session_id`, the session's recency metadata, and the turn entries in `turn_index` order; selector failure semantics stay deterministic (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `session-export <selector>` — export a persisted session bundle as deterministic JSON packaging session state plus transcript together; `<selector>` accepts raw `session_id`, `latest`, or `label:<name>` routed through the shared selector-resolution path; output confirms the `exported_session_id` as the actual resolved `session_id` (never the typed selector string) and preserves `turn_index` ordering so bundles can be archived, attached to bug reports, or compared across environments without manually inspecting `.sessions/` files; selector failure semantics stay deterministic (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `session-compare <left-selector> <right-selector>` — compare two persisted sessions as a single machine-readable JSON bundle; each positional argument independently accepts raw `session_id`, `latest`, or `label:<name>` routed through the shared selector-resolution path so the two sides can mix forms freely; output identifies both compared `session_id`s as the actual resolved values (never the typed selector strings) and reports signed `right - left` deltas for recency metadata (`created_at_ms_delta`, `updated_at_ms_delta`) and activity metadata (`message_count_delta`, `transcript_entry_count_delta`), plus a `same_session` flag so self-comparisons via `latest latest` are trivially recognizable; selector failure semantics stay deterministic and apply independently to each side (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `session-delete <selector>` — delete a persisted session cleanly; `<selector>` accepts raw `session_id`, `latest`, or `label:<name>` routed through the shared selector-resolution path; a single call removes both the session JSON and its sibling transcript JSON, and the output is deterministic machine-readable JSON that identifies the deleted session id (as the actual resolved `session_id`) plus the removed paths so the deletion is trivially auditable; selector failure semantics stay deterministic (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`), and the command fails without deleting anything else when the target session does not exist
- [x] `session-import <bundle-path>` — restore a persisted session from a JSON bundle previously emitted by `session-export`; accepts the deterministic `{ exported_session_id, session, transcript }` shape, recreates both persisted artifacts while preserving the imported session id, recency/activity metadata, and transcript `turn_index` ordering, and fails cleanly without overwriting unrelated persisted sessions when the bundle is invalid or the target session id already exists locally
- [x] `session-find <query>` — search persisted local sessions by transcript prompt text without mutating any session state; the query is matched case-insensitively as a substring against each persisted transcript entry; output is a deterministic JSON array ordered using the existing newest-first session ordering, where each result identifies the matched `session_id` plus recency/activity metadata and a `matches` array of `{ turn_index, prompt }` entries so the command is useful from the terminal without a follow-up `transcript-show`; an empty query and a query with no matches both succeed cleanly with an empty array instead of erroring
- [x] `session-rename <selector> <label>` — attach a trimmed, non-empty human-readable label to persisted session metadata while preserving the existing `session_id`, leaving transcript entries and ordering untouched, and not bumping `updated_at_ms` so newest-first ordering stays activity-based; `<selector>` accepts raw `session_id`, `latest`, or `label:<old-name>` routed through the shared selector-resolution path; output is a deterministic `{ renamed_session_id, applied_label }` JSON shape where `renamed_session_id` is the actual resolved `session_id` (never the typed selector string); empty/whitespace-only labels fail cleanly and selector failure semantics stay deterministic (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`); older unlabeled sessions remain readable because the label field is only emitted after a session has actually been labeled
- [x] `session-labels` — list every persisted session that currently carries a label without mutating any session state; output is a deterministic JSON array ordered using the same newest-first persisted-session ordering as `sessions`, each entry exposes `label`, `session_id`, `created_at_ms`, `updated_at_ms`, `message_count`, and `persisted_path`; unlabeled sessions are omitted, duplicate labels stay visible as separate rows so ambiguity is discoverable before a `label:<name>` selector would fail with `AmbiguousLabel`, and an empty labeled store returns a clean empty JSON array
- [x] `label:<name>` selector for persisted sessions accepted anywhere a single persisted session id is accepted (`session-show`, `transcript-show`, `resume`, `session-export`, `session-delete`, `session-fork`, `session-rename`, and either side of `session-compare`); selector resolution is centralized at the runtime layer (`RuntimeEngine::resolve_selector`) and delegates to `SessionStore::resolve_selector` so every command path resolves selectors uniformly; raw session ids and `latest` keep their existing behavior, machine-readable JSON outputs continue to surface the actual resolved `session_id`, and unknown labels (`SessionNotFound("label:<name>")`), ambiguous labels (`AmbiguousLabel`), and malformed selectors (`MalformedSelector` for `label:` with no name) all fail cleanly with distinct diagnostics; activity-based newest-first ordering is unchanged and mixed labeled/unlabeled stores stay backward-compatible
- [x] `session-unlabel <selector>` (and `session-unlabel latest` / `session-unlabel label:<name>`) — remove the persisted `label` metadata field from a session without disturbing `session_id`, transcript entries, transcript ordering, or `updated_at_ms`; the same shared selector machinery as every other single-session command accepts raw ids, `latest`, and `label:<name>`; output is a deterministic `{ unlabeled_session_id, removed_label }` JSON shape that identifies the resolved session id and the label that was cleared; unknown sessions/selectors surface as `SessionNotFound`, and attempting to unlabel a session that already carries no label surfaces as `SessionAlreadyUnlabeled` rather than a silent no-op; after removal the session disappears from `session-labels` while its persisted JSON no longer emits a `label` field, keeping older unlabeled sessions backward-compatible
- [x] `session-retag <selector> <label>` (and `session-retag latest <label>` / `session-retag label:<old-name> <new-name>`) — atomically replace the persisted `label` metadata field on a session in one step, preserving the existing `session_id`, leaving transcript entries and ordering untouched, and not bumping `updated_at_ms` so newest-first ordering stays activity-based; the same shared selector machinery as every other single-session command accepts raw ids, `latest`, and `label:<name>`; output is a deterministic `{ retagged_session_id, previous_label, applied_label }` JSON shape that identifies the resolved session id, the label that was replaced, and the normalized label now persisted; unknown sessions/selectors surface as `SessionNotFound`, empty/whitespace-only labels surface as `InvalidLabel`, retagging an unlabeled session surfaces as `SessionAlreadyUnlabeled`, and a same-effective-label request surfaces as `SessionAlreadyLabeled` rather than a silent no-op; the persisted JSON continues to serialize the `label` field only when present so older unlabeled sessions stay backward-compatible
- [x] `session-prune --keep <count>` — bulk-remove older *prune-eligible (unpinned)* persisted sessions, preserving the newest `<count>` unpinned sessions using the same newest-first ordering as `sessions` and `session-labels` (`updated_at_ms` → `created_at_ms` → `session_id` → `persisted_path`) applied only across the unpinned subset; pinned sessions are always preserved regardless of `<count>` and surface via `pinned_preserved_count` / `pinned_preserved`; for each pruned session both persisted artifacts are removed together — the `.sessions/<session-id>.json` file and its sibling `.sessions/<session-id>.transcript.json` — and the output is a deterministic `{ kept_count, pruned_count, pinned_preserved_count, removed, pinned_preserved }` JSON shape where `removed` lists each pruned session's `session_id` together with the removed session and transcript paths, and `pinned_preserved` lists every rescued pinned `session_id`; preserved sessions are never mutated — their label, pinned flag, transcript entries, transcript ordering, and activity metadata stay exactly as they were; `--keep 0` is supported and prunes every unpinned persisted session, and a store already within the retention budget after excluding pins succeeds cleanly with an empty `removed` array
- [x] `session-pin <selector>` / `session-unpin <selector>` — toggle a deterministic `pinned` flag on persisted session metadata while preserving the existing `session_id`, leaving transcript entries and ordering untouched, and not bumping `updated_at_ms` so newest-first ordering stays activity-based; `<selector>` accepts raw `session_id`, `latest`, or `label:<name>` routed through the shared selector-resolution path; output is a deterministic `{ pinned_session_id, pinned }` / `{ unpinned_session_id, pinned }` JSON shape where the resolved id is the actual persisted `session_id` (never the typed selector string); selector failure semantics stay deterministic (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`); already-pinned/unpinned states surface as `SessionAlreadyPinned` / `SessionAlreadyUnpinned` rather than silent no-ops; the persisted JSON serializes the `pinned` field only when the session is actually pinned so older sessions stay byte-compatible; pinned state also surfaces through `sessions`, `session-show`, `session-export`, `session-compare`, and `session-labels`, and `session-prune --keep <count>` excludes pinned sessions from pruning and surfaces them via `pinned_preserved_count` / `pinned_preserved`
- [x] `session-pins` — list every persisted session that is currently pinned without mutating any session state; output is a deterministic JSON array ordered using the same newest-first persisted-session ordering as `sessions` and `session-labels` (`updated_at_ms` → `created_at_ms` → `session_id` → `persisted_path`), each entry exposes `session_id`, `created_at_ms`, `updated_at_ms`, `message_count`, `persisted_path`, and `pinned: true`, plus `label` when the pinned session carries one so the listing is useful from the terminal without a follow-up `session-show`; unpinned sessions are omitted, duplicate labels on pinned sessions stay visible as separate rows so ambiguity is discoverable, and an empty pinned store returns a clean empty JSON array; pairs with `session-prune --keep <count>` so the protected set can be audited before a prune runs
- [x] `session-selector-check <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and surfaces the targeted persisted session's descriptive metadata without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, message_count, persisted_path, label?, pinned? }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `label` is only emitted when the targeted session carries one, and `pinned` is only emitted when `true`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`), so scripts can verify selector resolution before invoking any mutating command
- [x] `transcript-tail <selector>` with optional `--count <n>` (defaulting to `10`) — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns the newest `<n>` transcript entries for the resolved session in `turn_index` order without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, returned_entries, entries }` JSON shape where `selector` echoes the raw input, `total_entries` is the full transcript length, `returned_entries == entries.len()`, and each entry carries at least `turn_index` and `prompt`; a `--count` larger than the transcript returns every available entry, `--count 0` or an empty transcript returns an empty `entries` array cleanly, and negative / non-numeric `--count` values fail cleanly at parse time; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-find <selector> <query>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and searches only the resolved session's persisted transcript for entries whose prompt text contains `<query>` case-insensitively as a substring, mirroring `session-find` query semantics but scoped to a single selected session; output is a deterministic `{ selector, resolved_session_id, query, created_at_ms, updated_at_ms, total_entries, match_count, matches }` JSON shape where `selector` and `query` echo the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` is the full transcript length, `match_count == matches.len()`, and each entry in `matches` carries at least `turn_index` and `prompt` in the source transcript's `turn_index` order; an empty query and a no-match query both succeed cleanly with `match_count == 0` and an empty `matches` array; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`); does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata
- [x] `transcript-range <selector> --start <turn-index> [--count <n>]` (defaulting to `10`) — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a bounded forward slice of the resolved session's persisted transcript beginning at `turn_index == <start>` and containing at most `<n>` entries in `turn_index` order without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, start_turn_index, requested_count, created_at_ms, updated_at_ms, total_entries, returned_entries, entries }` JSON shape where `selector` echoes the raw input, `start_turn_index` and `requested_count` echo the requested window, `total_entries` is the full transcript length, `returned_entries == entries.len()`, and each entry carries at least `turn_index` and `prompt`; a `--count` larger than the remaining entries returns the available tail cleanly, a `--start` past the end of the transcript (or on an empty transcript) returns an empty `entries` array cleanly, `--count 0` returns an empty `entries` array cleanly, and negative / non-numeric `--start` or `--count` values fail cleanly at parse time; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-turn-show <selector> --turn <turn-index>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns the single persisted transcript entry whose `turn_index == <turn>` from the resolved session's transcript without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, turn_index, created_at_ms, updated_at_ms, total_entries, entry }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `turn_index` echoes the requested turn, `total_entries` is the full transcript length, and `entry` carries at least `turn_index` and `prompt`; because the contract is to return exactly one entry, an empty transcript and a `--turn` past the end of the transcript both fail cleanly and deterministically as `TranscriptTurnOutOfRange` rather than masking the miss as a silent empty result, and negative / non-numeric `--turn` values fail cleanly at parse time; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-last-turn <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns the single persisted transcript entry with the highest available `turn_index` from the resolved session's transcript without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, turn_index, created_at_ms, updated_at_ms, total_entries, entry }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `turn_index` is the newest transcript `turn_index` present, `total_entries` is the full transcript length, and `entry` carries at least `turn_index` and `prompt`; because the contract is to return exactly one entry, an empty transcript has no last turn and fails cleanly and deterministically as `TranscriptTurnOutOfRange` rather than masking the miss as a silent empty result; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-first-turn <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns the single persisted transcript entry with the lowest available `turn_index` from the resolved session's transcript without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, turn_index, created_at_ms, updated_at_ms, total_entries, entry }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `turn_index` is the oldest transcript `turn_index` present, `total_entries` is the full transcript length, and `entry` carries at least `turn_index` and `prompt`; because the contract is to return exactly one entry, an empty transcript has no first turn and fails cleanly and deterministically as `TranscriptTurnOutOfRange` rather than masking the miss as a silent empty result; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-entry-count <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary of the resolved session's persisted transcript length without returning any transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, and `total_entries` equals the persisted transcript length; empty transcripts succeed cleanly with `total_entries: 0` because the command's contract does not require returning an entry; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-has-entries <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary describing whether the resolved session's persisted transcript contains any entries without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, has_entries }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `has_entries` is `true` exactly when `total_entries > 0`; empty transcripts succeed cleanly with `total_entries: 0` and `has_entries: false`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-turn-exists <selector> --turn <turn-index>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary describing whether the resolved session's persisted transcript contains an entry whose `turn_index == <turn-index>` without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, turn_index, created_at_ms, updated_at_ms, total_entries, exists }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `turn_index` echoes the requested turn, `total_entries` equals the persisted transcript length, and `exists` is `true` exactly when the transcript contains an entry whose `turn_index == <turn-index>`; empty transcripts and out-of-range turns succeed cleanly with `exists: false`, negative / non-numeric `--turn` values fail cleanly at parse time, and existing selector failure semantics remain unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-turn-indexes <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary listing the `turn_index` values present in the resolved session's persisted transcript in ascending order without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, turn_indexes }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `turn_indexes` is an ascending array of the `turn_index` values present in the persisted transcript; empty transcripts succeed cleanly with `total_entries: 0` and `turn_indexes: []`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-turn-index-range <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary describing the lowest and highest `turn_index` values present in the resolved session's persisted transcript without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, first_turn_index, last_turn_index }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `first_turn_index` / `last_turn_index` are the smallest and largest present `turn_index` values in the persisted transcript; empty transcripts succeed cleanly with `total_entries: 0` and both bounds `null`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-has-turn-gaps <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary describing whether the resolved session's persisted transcript has at least one missing integer `turn_index` between the smallest and largest present `turn_index` values without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, has_turn_gaps }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `has_turn_gaps` is `true` exactly when at least one integer `turn_index` between the smallest and largest present `turn_index` values is missing from the resolved persisted transcript; empty and single-entry transcripts succeed cleanly with `has_turn_gaps: false`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-missing-turn-indexes <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary listing every missing integer `turn_index` between the smallest and largest present `turn_index` values in the resolved session's persisted transcript in ascending order without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, missing_turn_indexes }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `missing_turn_indexes` is an ascending array of every missing integer `turn_index` between the smallest and largest present `turn_index` values in the persisted transcript; empty, single-entry, and contiguous transcripts succeed cleanly with `missing_turn_indexes: []`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-turn-density <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary describing how densely populated the resolved session's persisted transcript's `turn_index` span is without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, span_entry_count, missing_turn_count, turn_density }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, `span_entry_count` is the number of integer turn positions in the inclusive span from the smallest present `turn_index` to the largest present `turn_index`, `missing_turn_count` equals `span_entry_count - total_entries`, and `turn_density` equals `total_entries / span_entry_count` as a deterministic numeric value; empty transcripts succeed cleanly with `total_entries: 0`, `span_entry_count: 0`, `missing_turn_count: 0`, and `turn_density: 1.0`; single-entry and contiguous transcripts succeed cleanly with `missing_turn_count: 0` and `turn_density: 1.0`; gapped transcripts report `turn_density` strictly less than `1.0`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-gap-ranges <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary describing every contiguous run of missing integer `turn_index` values between the smallest and largest present `turn_index` values in the resolved session's persisted transcript, in ascending order, without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, gap_ranges }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `gap_ranges` is an ascending array of contiguous missing-turn runs, each range carrying `{ start_turn_index, end_turn_index, missing_count }` with inclusive bounds; single missing turns collapse to a range where `start_turn_index == end_turn_index` and `missing_count == 1`; adjacent missing turns collapse into a single range and disjoint gaps produce multiple ascending ranges; empty, single-entry, and contiguous transcripts succeed cleanly with `gap_ranges: []`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-largest-gap <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary describing only the single largest contiguous run of missing integer `turn_index` values between the smallest and largest present `turn_index` values in the resolved session's persisted transcript, without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, largest_gap }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `largest_gap` is either `null` when no gap exists or an object carrying `{ start_turn_index, end_turn_index, missing_count }` with inclusive bounds; a single missing turn collapses to a run where `start_turn_index == end_turn_index` and `missing_count == 1`; when multiple gap runs tie for the highest `missing_count`, the earliest run (lowest `start_turn_index`) wins deterministically; empty transcripts, single-entry transcripts, and contiguous transcripts succeed cleanly with `largest_gap: null`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-smallest-gap <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable summary describing only the single smallest contiguous run of missing integer `turn_index` values between the smallest and largest present `turn_index` values in the resolved session's persisted transcript, without returning transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, smallest_gap }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `smallest_gap` is either `null` when no gap exists or an object carrying `{ start_turn_index, end_turn_index, missing_count }` with inclusive bounds; a single missing turn collapses to a run where `start_turn_index == end_turn_index` and `missing_count == 1`; when multiple gap runs tie for the lowest `missing_count`, the earliest run (lowest `start_turn_index`) wins deterministically; empty transcripts, single-entry transcripts, and contiguous transcripts succeed cleanly with `smallest_gap: null`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-gap-count <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable count of contiguous runs of missing integer `turn_index` values between the smallest and largest present `turn_index` values in the resolved session's persisted transcript without returning the runs themselves or any transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, gap_count }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `gap_count` is the number of contiguous missing-`turn_index` runs — adjacent missing turns collapse into one run and disjoint runs count separately; empty, single-entry, and contiguous transcripts succeed cleanly with `gap_count: 0`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-missing-turn-count <selector>` — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a deterministic machine-readable count of individual missing integer `turn_index` values between the smallest and largest present `turn_index` values in the resolved session's persisted transcript without returning the missing indexes themselves or any transcript entries and without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, missing_turn_count }` JSON shape where `selector` echoes the raw input, `resolved_session_id` is the persisted id the selector actually maps to, `total_entries` equals the persisted transcript length, and `missing_turn_count` is the number of individual missing integer `turn_index` positions — every missing integer between the smallest and largest present `turn_index` values contributes `1` and disjoint runs sum additively; empty, single-entry, and contiguous transcripts succeed cleanly with `missing_turn_count: 0`; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `sessions [--limit <n>]` — inspect-only listing with an optional `--limit <n>` flag that preserves the existing newest-first persisted-session ordering (`updated_at_ms` → `created_at_ms` → `session_id` → `persisted_path`); omitting `--limit` returns every persisted session in the existing deterministic JSON array shape unchanged, `--limit <n>` returns at most the newest `n` rows from the same ordering, `--limit 0` returns an empty array cleanly, a `--limit` greater than the number of persisted sessions returns every available session cleanly, and the per-row JSON shape is preserved — the limited form is a slice of the existing array rather than a new wrapper object; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time
- [x] `session-labels [--limit <n>]` — inspect-only listing with an optional `--limit <n>` flag that preserves the existing newest-first persisted-session ordering (`updated_at_ms` → `created_at_ms` → `session_id` → `persisted_path`) and existing labeled-session filtering (only labeled sessions returned, unlabeled omitted, duplicate labels visible as separate rows); omitting `--limit` returns every labeled persisted session in the existing deterministic JSON array shape unchanged, `--limit <n>` returns at most the newest `n` rows from the same ordering and filtered subset, `--limit 0` returns an empty array cleanly, a `--limit` greater than the labeled subset size returns every labeled session cleanly, and the per-row JSON shape is preserved — the limited form is a slice of the existing array rather than a new wrapper object; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time
- [x] `session-pins [--limit <n>]` — inspect-only listing with an optional `--limit <n>` flag that preserves the existing newest-first persisted-session ordering (`updated_at_ms` → `created_at_ms` → `session_id` → `persisted_path`) and existing pinned-session filtering (only pinned sessions returned, unpinned omitted, duplicate labels on pinned sessions still visible as separate rows); omitting `--limit` returns every pinned persisted session in the existing deterministic JSON array shape unchanged, `--limit <n>` returns at most the newest `n` rows from the same ordering and filtered subset, `--limit 0` returns an empty array cleanly, a `--limit` greater than the pinned subset size returns every pinned session cleanly, and the per-row JSON shape is preserved — the limited form is a slice of the existing array rather than a new wrapper object; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time
- [x] `session-find <query> [--limit <n>]` — inspect-only persisted-session search with an optional `--limit <n>` flag that preserves the existing query semantics (case-insensitive substring match against persisted transcript prompt text; empty query and no-match query remain a clean empty array), the existing newest-first persisted-session ordering (`updated_at_ms` → `created_at_ms` → `session_id` → `persisted_path`), and the existing result filtering (only sessions with at least one matching transcript entry are returned, and each returned row preserves its existing per-row JSON shape including the `matches` array in `turn_index` order); omitting `--limit` returns every matching persisted session in the existing deterministic JSON array shape unchanged, `--limit <n>` returns at most the newest `n` rows from the same ordering and matched subset, `--limit 0` returns an empty array cleanly, a `--limit` greater than the matched subset size returns every matching session cleanly, and the per-row JSON shape is preserved — the limited form is a slice of the existing array rather than a new wrapper object; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time
- [x] `transcript-context <selector> --turn <turn-index> [--before <n>] [--after <n>]` (each defaulting to `2`) — inspect-only command that resolves a single-session selector (raw id, `latest`, or `label:<name>`) through the shared selector-resolution path and returns a bounded symmetric window around `turn_index == <turn>` from the resolved session's persisted transcript, including the centered entry when present plus up to `<before>` preceding and `<after>` following entries in `turn_index` order without mutating any persisted state, transcript entry, label, pinned flag, id, path, or ordering metadata; output is a deterministic `{ selector, resolved_session_id, center_turn_index, requested_before, requested_after, created_at_ms, updated_at_ms, total_entries, returned_entries, entries }` JSON shape where `selector` echoes the raw input, `center_turn_index`, `requested_before`, and `requested_after` echo the requested window, `total_entries` is the full transcript length, `returned_entries == entries.len()`, and each entry carries at least `turn_index` and `prompt`; a window extending past either transcript bound is clipped cleanly to the available in-range entries, a `--turn` past the end of the transcript (or on an empty transcript) returns an empty `entries` array cleanly, and negative / non-numeric `--turn`, `--before`, or `--after` values fail cleanly at parse time; preserves existing selector failure semantics unchanged (unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`)
- [x] `transcript-find <selector> <query> [--limit <n>]` — inspect-only transcript search with an optional `--limit <n>` flag that preserves the existing selector semantics (raw id, `latest`, and `label:<name>` resolved via the shared selector-resolution path with unchanged failure semantics — unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`), the existing query semantics (case-insensitive substring match against persisted transcript prompt text; empty query and no-match query still succeed cleanly with `match_count: 0` and an empty `matches` array), and the existing result ordering (source transcript `turn_index` order inside `matches`); omitting `--limit` returns every matching transcript entry in the existing deterministic `{ selector, resolved_session_id, query, created_at_ms, updated_at_ms, total_entries, match_count, matches }` JSON shape unchanged, `--limit <n>` truncates `matches` to at most the first `n` entries in the same `turn_index` ordering and recomputes `match_count` so `match_count == matches.len()` still holds, `--limit 0` returns `match_count: 0` with an empty `matches` array cleanly, a `--limit` greater than the match count returns every match cleanly, and the outer JSON shape is preserved — the limited form is a truncation of the existing `matches` array rather than a new wrapper object; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time
- [x] `transcript-turn-indexes <selector> [--limit <n>]` — inspect-only transcript turn-index listing with an optional `--limit <n>` flag that preserves the existing selector semantics (raw id, `latest`, and `label:<name>` resolved via the shared selector-resolution path with unchanged failure semantics — unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`) and the existing ascending `turn_index` ordering inside `turn_indexes`; omitting `--limit` returns every present `turn_index` in the existing deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, turn_indexes }` JSON shape unchanged, `--limit <n>` truncates `turn_indexes` to at most the first `n` entries in the same ascending order while `total_entries` continues to reflect the full persisted transcript length, `--limit 0` returns `turn_indexes: []` cleanly, a `--limit` greater than `total_entries` returns every present index cleanly, and the outer JSON shape is preserved — the limited form is a truncation of the existing `turn_indexes` array rather than a new wrapper object; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time
- [x] `transcript-missing-turn-indexes <selector> [--limit <n>]` — inspect-only transcript missing-turn-index listing with an optional `--limit <n>` flag that preserves the existing selector semantics (raw id, `latest`, and `label:<name>` resolved via the shared selector-resolution path with unchanged failure semantics — unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`) and the existing ascending `turn_index` ordering inside `missing_turn_indexes`; omitting `--limit` returns every missing `turn_index` in the existing deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, missing_turn_indexes }` JSON shape unchanged, `--limit <n>` truncates `missing_turn_indexes` to at most the first `n` entries in the same ascending order while `total_entries` continues to reflect the full persisted transcript length, `--limit 0` returns `missing_turn_indexes: []` cleanly, a `--limit` greater than the number of missing indexes returns every missing index cleanly, and the outer JSON shape is preserved — the limited form is a truncation of the existing `missing_turn_indexes` array rather than a new wrapper object; empty, single-entry, and contiguous transcripts continue to succeed cleanly with `missing_turn_indexes: []` with and without `--limit`; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time
- [x] `transcript-gap-ranges <selector> [--limit <n>]` — inspect-only transcript gap-range listing with an optional `--limit <n>` flag that preserves the existing selector semantics (raw id, `latest`, and `label:<name>` resolved via the shared selector-resolution path with unchanged failure semantics — unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`) and the existing ascending gap-range ordering inside `gap_ranges` (each range carrying `{ start_turn_index, end_turn_index, missing_count }` with inclusive bounds and the existing collapse semantics for single/adjacent/disjoint missing turns); omitting `--limit` returns every gap range in the existing deterministic `{ selector, resolved_session_id, created_at_ms, updated_at_ms, total_entries, gap_ranges }` JSON shape unchanged, `--limit <n>` truncates `gap_ranges` to at most the first `n` entries in the same ascending order while `total_entries` continues to reflect the full persisted transcript length, `--limit 0` returns `gap_ranges: []` cleanly, a `--limit` greater than the number of gap ranges returns every gap range cleanly, and the outer JSON shape is preserved — the limited form is a truncation of the existing `gap_ranges` array rather than a new wrapper object; empty, single-entry, and contiguous transcripts continue to succeed cleanly with `gap_ranges: []` with and without `--limit`; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time
- [x] `transcript-show <selector> [--limit <n>]` — inspect-only bounded transcript inspection with an optional `--limit <n>` flag that preserves the existing selector semantics (raw id, `latest`, and `label:<name>` resolved via the shared selector-resolution path with unchanged failure semantics — unknown id/label → `SessionNotFound`, duplicate labels → `AmbiguousLabel`, empty `label:` → `MalformedSelector`) and the existing canonical append-order ordering inside `entries`; omitting `--limit` returns every persisted transcript entry in the existing deterministic `{ session_id, created_at_ms, updated_at_ms, entries }` JSON shape unchanged, `--limit <n>` truncates `entries` to at most the first `n` entries in the same canonical append order while the outer JSON shape is preserved — the limited form is a truncation of the existing `entries` array rather than a new wrapper object, `--limit 0` returns `entries: []` cleanly, a `--limit` greater than the persisted transcript length returns every entry cleanly, and empty transcripts continue to succeed cleanly with `entries: []` with and without `--limit`; does not mutate any persisted session, transcript entry, label, pinned flag, id, path, or ordering metadata, and negative / non-numeric `--limit` values fail cleanly at parse time

## Phase 8 - Cleanup

- [x] Remove obsolete Python-first scaffolding
- [x] Rewrite README for Rust-first identity
- [x] Add examples and usage docs

## Engineering Rules

- Build Rust-native modules, not Python transliterations
- Keep modules small and typed
- Prefer enums/structs over arbitrary strings
- Separate pure logic from filesystem operations
- Make runtime behavior inspectable through events
- Prioritize primary Claude Code CLI usability first, while preserving donor value for Horizon where it does not fight the product path

## Immediate Next Slice

1. expand README/ARCHITECTURE examples when the visible CLI surface changes beyond the current seeded baseline
2. keep README-backed CLI example tests updated whenever documented output changes
3. keep each follow-up slice tied to a GitHub issue and PR
4. prioritize slices that improve primary Claude Code CLI usability, not just internal donor value
